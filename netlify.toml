# Netlify build config — for now the site is plain static files (no build step).
# When we add the CMS build, this file gets updated automatically via a commit.
[build]
  publish = "."
  command = ""
